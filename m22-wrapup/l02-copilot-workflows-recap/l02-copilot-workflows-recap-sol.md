# Lab 22.2 - Lösung: Copilot-Workflows-Rückblick

## Aufgabe 1-3: Beispielhafte Fundstellen

| Stelle                                  | Vorschlag                                  | Übernommen?                     | Kategorie         |
| --------------------------------------- | ------------------------------------------ | ------------------------------- | ----------------- |
| `PATCH /tickets/:id/status` (Modul 14)  | Grundgerüst ähnlich zu `PATCH .../assign`  | angepasst (Statuslogik ergänzt) | Express-Routen    |
| `TicketCard`-Props-Interface (Modul 18) | Feldnamen basierend auf `Ticket`-Interface | unverändert übernommen          | React-Komponenten |

## Aufgabe 4: Eigene Regel (Beispiel)

Beispielhafte Regel: "Bei allem, was mit Passwörtern, Tokens oder Authentifizierung zu tun hat (wie in Modul 16), lese ich jede vorgeschlagene Zeile bewusst, bevor ich sie übernehme - bei reinen UI-Strukturvorschlägen (wie in Modul 18) reicht ein schnellerer Blick."

## Grenzen

Diese Übung ist bewusst individuell - es gibt keine "richtige" Musterlösung, sondern nur plausible, persönlich nachvollziehbare Beispiele.
